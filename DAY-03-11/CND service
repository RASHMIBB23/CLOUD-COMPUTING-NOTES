what is CND

## content deleivery network.

- to perform the speed of the website we have the keycn.

* what is cloudfront?
  -> aws cloudfront is a delivery network (cdn) service that delivers content globally with low latency and hight transfer speeds. it cahes content at edge locations close to users.

USE CASE: IT is ideal for delivering static files like html ,css,js ,images,and videos.
\*it also used apis or dynamic content with customizable caching.

---

## KEY CONCEPTS-

Edge locations: Global data centers where content is cahed.
Distributions: The cloudfront resource that tells how content will be delivered.
Origins: The locations of your content (e.g ,S3 buckets or web service)
=========================================================================

# SETTING UP A DEMO

- it delivers the data globally with low latency and high transfer speeds.
  delevers the data with low latency and high transfer speeds.
- IT cahes content at edge locations close to users.

USE CASE: it is ideal
when the website is hoster in broswer

- it takes the image from s3 bucket and move to the and after that it moves to the dge server and shows the content and after some time when u need to see again it shwos from the egde server not from the origin server so u can easily access the content
